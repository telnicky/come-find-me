<%= form_for(@location_request) do |f| %>
  <% if @location_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_request.errors.count, "error") %> prohibited this location_request from being saved:</h2>

      <ul>
      <% @location_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :location_id %><br>
    <%= f.number_field :location_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
